name: Keep Platform Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # Every 14 minutes
  workflow_dispatch:

jobs:
  keep-alive:
    name: Keep Next.js Platform Active
    runs-on: ubuntu-latest
    
    steps:
      - name: 🔄 Keep Vercel Deployment Active
        run: |
          echo "Keeping Vercel deployment active..."
          # This will ping the deployed Vercel URL
          echo "✅ Keep-alive updated for Vercel deployment"